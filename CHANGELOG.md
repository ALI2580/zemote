# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [0.6.0] - 2026-09-06

### Added
- **消息操作能力对齐官方**：assistant 回复的操作行新增复制与分叉按钮（复制全文到剪贴板；分叉调用 `forkAssistant`，新会话自动出现在任务列表）；用户气泡下方新增显性操作行——编辑并重发 / 复制 / 分叉，不再依赖不易发现的长按菜单；长按菜单同步补充"复制"项。

### Fixed
- **思考行与工具行靠左显示**：执行轨迹展开区此前被 Flutter ExpansionTile 的默认水平居中对齐影响，思考行、工具行现在与其他操作行一致靠左。
- **思考行箭头策略**：箭头默认隐藏，展开/收起操作后短暂出现并自动淡出（流式思考期间保持显示），对齐官方的悬浮显示策略在移动端的等效实现。
- turn 头部耗时不再出现 53m60s 式的进位错误（随 0.5.9 后的实测修复一并发布）。

## [0.5.9] - 2026-09-06

### Changed
- **思考块对齐官方浅色实测形态**：移除卡片容器，改为官方的无底色无框纯文本行「图标 思考 · 时长」，展开内容挂浅灰左竖线下。
- **工具行进一步对齐官方实测**：按工具类型使用专属图标（终端 / 放大镜 / 铅笔等），Bash 的中文类型修正为「终端」；已完成工具不再显示状态文字（仅进行中 / 待确认 / 失败显示）；编辑与写入行尾部显示 `+N -N` 增删行数角标（官方 diffCount）。
- **权限确认选项本地化**：服务端英文选项标签按官方映射表转为中文（允许一次 / 始终允许本项目 / 允许本会话等），并按官方 Fct 规则补齐各类型的本地描述文案。
- 修复 turn 头部耗时进位错误（如 53m60s 应为 54m0s）。

## [0.5.8] - 2026-09-06

### Changed
- **工具调用对齐官方行式布局**：移除工具卡片容器，改为官方 ToolLayout 的单行形态——图标、工具类型标签、`·` 分隔、参数摘要、状态标签全部 subtle 灰；工具类型中文化（读取 / 搜索 / 写入 / 编辑 / 命令等），状态文案对齐官方（等待中 / 执行中 / 已执行 / 执行失败 / 已拒绝 / 已停止）；运行中默认展开详情，详情区使用官方的左竖线缩进。
- **Diff 视图对齐官方行规格**：增删行改为官方的 14% 色底 + 左 3px 内嵌色条，行文本回归正常前景色（色彩只体现在底色与色条）；新增 `--color-diff-added/removed` 官方色板（深色 `#46BF72`/`#FF5C5C`，浅色 `#1E8A3E`/`#E03131`）。
- **权限确认卡对齐官方 elicitation 卡片**：绿色系确认色（`#87D9A4`）替代警告橙，16px 圆角实色卡片，选项改为官方的竖排全宽按钮行（序号 + 标签 + 描述），标题栏右侧显示工具名徽章。
- 时间线标记改为官方的双细线夹中央标签形态；turn 头部改为左对齐 subtle 小字 + 底部发丝线；子代理行对齐官方的单行 subtle 文本。
- 项目认知新增官方 UI 逆向规格表（工具卡片 / diff / 权限卡 / 时间线的完整 class 级规格）。

## [0.5.7] - 2026-09-06

### Added
- **@ 引用上下文（对齐官方 Web 端 mention 能力）**：输入框中键入 `@` 可引用工作区文件、其他会话与技能；`#` 可引用会话。选中后按官方格式以 markdown 链接注入消息文本（文件 `[名称](./相对路径)`、会话 `[#标题](#会话ID)`），桌面端原生解析。
- 引用菜单按类目分组展示（文件 / 会话 / 技能），与官方触发规则一致（`@`/`#` 须紧跟行首或空白）；文件列表通过桌面端 `fileService.readdir` 服务按需加载，加载失败时仅隐藏文件类目，不影响会话与技能（双数据源独立降级）。
- 协议层新增 `readdir` 镜像（`file` channel 的 `fileService.readdir`），响应形状宽松解析，未知结构安全降级为空列表。

### Changed
- **对话页消息区第一批对齐官方样式**：用户消息气泡改为官方形态（表面底色 + 10% 发丝边框 + 12px 圆角、右上角 2px 尖角、16/12 内边距、576px 宽上限），不再使用蓝色高亮气泡。
- 思考折叠块对齐官方：表面底色体系、紫色（`#A78BFA`）思考标记、标题文案统一为"思考"。
- `theme.dart` 新增官方 CSS 变量直译常量（消息表面 / 发丝边框 / 时间线轨迹五色），供后续批次复用。
- 逆向笔记沉淀至项目认知 `references/official-web-ui.md`（mention 格式表、主题变量表、官方样式常量与待挖清单）。

## [0.5.6] - 2026-09-06

### Added
- **输入框统一改造（对齐官方 Web 端 composer）**：输入区与功能工具条合并为同一个 16px 圆角容器，协作模式、模型、思考强度全部内联进输入框，不再隔离在框外。
- 协作模式下拉采用官方命名与图标：变更前确认 / 自动编辑 / 计划模式 / 完全访问，每项带说明副标题；"完全访问"整组浅橙加重（官方双色调：深色 #FF8A30 / 浅色 #E07B00）。
- 模型下拉按官方结构重组：当前供应商的模型直接平铺（带能力标签与选中勾），其他供应商点击后进入二级列表，窄屏下在面板内切换。
- 思考强度内联下拉（低 / 高 / 最高等，按桌面端下发的选项展示）。

### Changed
- 发送按钮由圆形改为官方的圆角方形样式；语音按钮移入输入行右侧；顶部"模型 / 模式"入口改为"更多设置"，原弹层保留作为其他配置与后续消息的入口。
- 新增锚定式下拉组件（贴近触发按钮向上弹出，点击外部关闭），替代原底部弹层作为模式/模型/思考的交互形态。

## [0.5.5] - 2026-09-06

### Added
- **ColorOS 16 流体云上岛（Android 16 Live Updates）**：任务运行中的常驻通知升级为 promoted ongoing——状态栏 status chip、通知抽屉顶部置顶、锁屏显著展示；任务进度以 `ProgressStyle` 彩色进度条呈现。
- 多任务并发时进度条显示"已完成 / 本轮出现任务数"的真实比例；单任务运行时展示活动条。老系统（Android 16 以下）保持原有常驻通知形态，两套自动切换。
- 声明 `POST_PROMOTED_NOTIFICATIONS` 权限；`compileSdk` 固定为 36 以使用 Live Updates API。

## [0.5.4] - 2026-09-06

### Added
- **辅助对话宽屏适配**：平板 / 展开态折叠屏（宽度 ≥ 640dp）从右侧滑出弹出层承载辅助对话，主对话保持可见；窄屏维持全页跳转。
- 模型选择改为手风琴样式：点击供应商原地展开该供应商的模型列表（同时只展开一个，带展开/收起动画）；单一供应商时退化为平铺直选。

### Changed
- 协作模式改为中文说明：构建 / 编辑 / 计划 / Full Access；Full Access 使用浅橙色加重展示（背景、边框、文字），与其余模式形成明显区分。
- 消息列表滚动判定优化：吸底/取消吸底只由用户的真实拖动或惯性滚动决定，程序自动跟随动画不再干扰判定。

### Fixed
- 流式输出期间向上翻阅历史消息不再被拉扯：自动跟随仅在吸底状态生效，移除"接近底部即跟随"的距离启发式；回到底部后自动恢复跟随。
- 离开对话页再返回，输入框保留之前的输入内容（草稿按会话保留；消息发出后草稿清除，新建会话不会冒出已发送内容）。

## [0.5.3] - 2026-09-05

### Changed
- **模型选择改为按供应商二级选择**：模型与模式弹窗中，模型列表先按供应商分组展示（显示供应商名称、模型数量和当前使用的模型），点击供应商进入二级列表选择具体模型；返回键可回到供应商列表。
- 仅存在单一供应商（或桌面未提供供应商信息）时自动退化为原有平铺直选，不增加多余点击。
- 模型切换的思考等级兼容逻辑、乐观更新与被拒绝提示行为保持不变。

## [0.5.2] - 2026-09-01

### Changed
- 任务页改为紧凑双行头部，搜索与状态筛选按需展开，任务列表占据页面主体空间。
- 升级任务卡片状态图标、状态标签、元信息和空状态表现。
- 提高深色和浅色主题下辅助文字、Tab、状态标签、输入提示和 Diff 的对比度。

### Fixed
- 修复任务首页固定模块过多导致任务列表只能显示半屏的问题。
- 修复浅色主题下状态文字、骨架屏和 Diff 内容不清晰的问题。
- 修复 Beta 更新检查使用旧版本常量导致重复提示同一版本的问题。

## [0.5.2-beta.3] - 2026-09-01

### Changed
- 将任务页固定头部压缩为标题栏和分类栏两行，任务列表重新占据页面主体空间。
- 搜索改为在标题栏内按需展开，状态筛选改为底部弹层，新建任务保留为紧凑图标入口。
- 提高深色和浅色主题下辅助文字、Tab、状态标签和输入提示的对比度。
- Diff 与命令建议面板改为完整适配深色和浅色主题。

### Fixed
- 修复浅色主题下状态文字、骨架屏和 Diff 内容不清晰的问题。
- 修复任务首页固定模块过多导致任务列表只能显示半屏的问题。

## [0.5.2-beta.2] - 2026-09-01

### Fixed
- 修复更新检查仍使用旧版硬编码版本号，安装 Beta 后会重复提示同一版本的问题。

## [0.5.2-beta.1] - 2026-09-01

### Changed
- 重构任务首页信息层级，突出新建任务和正在运行的任务。
- 收拢低频操作入口，减少首页顶部工具栏噪音。
- 升级任务卡片状态图标、状态标签、元信息和空状态表现。
- 统一深色/浅色主题的导航栏、Tab、筛选 Chip 和按钮样式。

### Fixed
- 任务状态文案不再直接显示内部状态值，例如 `completedSuccess`。

## [0.5.1] - 2026-08-31

### Added
- 聊天输入栏改为微信式布局：加号菜单集中上传文件和选择 Skill，麦克风独立位于输入框右侧。
- 录音期间持续显示离线识别预览，停止录音后保留最终识别文字。
- 增加 Qwen3-ASR 0.6B 和 Fun-ASR Nano 离线模型备选。

## [0.5.1-beta.2] - 2026-08-31

### Fixed
- 修复从其他应用返回、锁屏解锁后连接停在重连中无法恢复的问题。
- 修复多个重连请求并发时旧 WebSocket 事件覆盖新连接状态的问题。
- 修复连接冲突被误判为永久“被踢下线”的问题，认证阶段会先执行一次干净重连。

### Added
- 增加跨平台离线语音输入基础能力和多模型管理入口。
- 设置页支持下载、启用和删除离线语音识别模型。

## [0.5.1-beta.1] - 2026-08-28

### Added
- 会话工作台改为紧凑状态总览条，减少对聊天内容的占用。
- 计划、文件变更和后台任务改为底部标签面板，支持快速切换查看。
- 文件变更摘要显示文件数量、新增行和删除行。
- 设置页显示“接收 Beta 更新”开关，可选择稳定版或稳定版 + Beta 版通道。

### Fixed
- 修复 Beta 更新开关已实现但未加入设置页导致用户不可见的问题。

## [0.5.0] - 2026-08-28

### Added
- 设置页新增“接收 Beta 更新”开关，默认只接收稳定版；开启后同时接收 Beta/RC 预发布版本。
- 更新检测支持 `vX.Y.Z-beta.N`、`vX.Y.Z-rc.N` 预发布版本排序和 GitHub Pre-release 过滤。
- 稳定版和 Beta 版沿用同一应用与签名，分别通过 `main`、`beta` 分支打标签发布。
- 计划模式下在聊天页实时显示执行计划、步骤状态和完成进度。
- 计划内容同时支持会话快照、`conversationPlansV4` 返回和 `update_plan` / `TodoWrite` 工具调用解析。
- 切换计划模式或计划 revision 更新时自动刷新计划数据，无法识别结构时可查看完整原始计划。
- 会话列表增加按工作区隔离的本地缓存；设置页新增诊断中心。

## [0.4.4] - 2026-08-27

### Fixed
- 思考过程改为默认展开的高对比面板，浅色和深色主题下均使用明确背景、边框和可读文字。
- 工具调用卡片提升背景与边框对比度，运行中的工具默认展开，输入/输出内容更易辨识。
- Markdown 标题、列表和表格显式使用主题文字色，修复部分主题下文字与背景对比不足。

## [0.4.3] - 2026-08-27

### Fixed
- 修复桌面发送的合法单元素 Channel Initialize 帧 `[200]` 被丢弃，导致 `listTasks` 和 sessions-index 永远等待、会话列表无数据的问题。
- Conversation V4 握手恢复协议能力版本 `3.6.5`，不再错误使用 Zemote 应用版本参与桌面能力协商。
- channel 任务数据与 sessions-index 实时数据改为独立保存再合并，空快照、晚到响应和订阅失败不再互相清空会话列表。

### Tests
- 使用真实桌面连接完成只读验证：`listTasks=1`、sessions-index `=2`、最终可见会话 `=2`。
- 新增真实会话列表探针及 Initialize 单元素帧、双数据源空响应回归测试。

## [0.4.2] - 2026-08-27

### Added
- Android 更新按 CPU 架构自动选择安装包，发布产物拆分为 `arm64-v8a`、`armeabi-v7a` 和 `x86_64`。
- 每个 APK 发布独立 `.md5` 校验文件，应用安装前执行 MD5 完整性验证。
- 下载支持断点续传；本地已存在且 MD5 正确的安装包会跳过下载并直接打开安装器。
- 应用升级完成后通过 `MY_PACKAGE_REPLACED` 自动删除已使用的 APK 和更新缓存。

### Fixed
- sessions-index 快照到达后立即结束会话列表加载，不再被旧任务 RPC 的超时骨架屏遮住；空列表也能正常结束加载。
- Conversation V4 握手恢复协议兼容版本 `3.6.5`，避免错误使用 Zemote `0.x` 版本导致 sessions-index 能力协商异常。
- 修复 Channel 合法单元素 Initialize 帧 `[200]` 被误判为畸形帧的问题；该回归会导致所有任务 RPC 超时、会话列表无数据。

### Changed
- 更新 APK 改为存放在应用内部 `files/update` 目录，FileProvider 仅暴露该目录。
- Release 现在上传 3 个 ABI APK 及对应的 3 个 MD5 文件。

## [0.4.1] - 2026-08-27

### Added
- 历史消息分页使用已加载的最旧消息作为游标，重同步时保留已加载历史。
- 文本消息本地回显、发送状态和失败重试。
- 前台恢复时主动探测 relay，心跳超时先探测再重连。
- 更新 APK 的 SHA-256 校验、断点续传和发布 workflow 的 Web 冒烟检查。
- 崩溃留痕、详细 relay 帧日志开关和 Android 禁止备份凭据配置。

### Fixed
- 修正 AI 多问题交互答案为一次性 `accept` 提交格式。
- 拒绝非 HTTPS/WSS 的设备连接 URL。
- 从聊天页返回后按当前工作区 sessions-index 重新合并会话列表，避免缺项、旧顺序和跨工作区任务混入。
- 打开聊天页并完成初始历史加载后强制定位到最新消息。
- 订阅初始化失败时清理事件监听和定时器，异常分片不再中断订阅。
- RPC/IPC 增加分片数量、消息大小、校验长度和畸形帧边界检查。

## [0.4.0] - 2026-08-27

### Changed
- 版本号升级至 `0.4.0+1`。

## [0.3.5] - 2026-08-07

### Fixed
- 会话首次订阅（冷启动运行预热）放宽至 60s，避免打开聊天/恢复时超时误判「连不上」。
- relay 心跳超时触发重连时正确触发 bridge 恢复（此前跳过 `reconnecting` 状态导致配对后 bridge 不恢复）。
- 打开聊天页显式定位到最新消息（此前监听器错过初始快照）。
- 气泡保留原始顺序（思考→文本→工具→文本…），点赞区只在回复最后一个文本段显示一次。

## [0.3.4] - 2026-08-07

### Fixed
- **气泡内容顺序错误**：思考过程/工具调用与总结顺序被颠倒。已改为保留原始顺序（思考 → 文本 → 工具 → 文本…），连续文本合并，点赞区只在回复最后一个文本段显示一次。
- 打开聊天页时定位到最新消息（底部）；向上翻阅历史时流式更新不再拉扯；加载更早消息后若在底部自动回到最新。

## [0.3.3] - 2026-08-07

### Added
- **设备列表导入/导出**：导出全部设备（JSON 文件，含连接 URL，带凭据安全提示）；可从文件导入，自动跳过无效/重复设备。
- **同一条回复合并为单条气泡**：交错 tool/reasoning 时文本合并为一个气泡（一个点赞区）。

### Fixed
- 服务端 `bridge-degraded` 恢复失败不再卡死（并入重试循环）。
- `sendText` 在连接健康时超时不再重复发送（仅断线中自动重试）。
- `ZemoteClient.dispose()` 释放活动桥，修复连接泄漏。
- 断线重连：bridge 恢复持续重试直到成功；relay 重连后卡在 waiting 自动强制重连；聊天页显示「正在自动重连」提示条。
- 聊天配色（浅色主题代码块/推理/工具卡片）改为主题感知。
- AI 询问用户（交互）按官方 schema 修正权限选项与自由输入，新增 `questions` 表单。
- 新建会话首条消息随 `createSession(firstInput)` 发送，发送 ack 失败有明确提示。

## [0.3.2] - 2026-08-07

### Fixed
- **断线后发消息超时**：relay 断开时立即标记 bridge 降级，命令在恢复前排队等待（`waitHealthy`）；发送超时后等待重连并自动重试一次；聊天页新增「正在自动重连」提示条。
- **聊天配色异常（尤其浅色主题代码块）**：markdown 代码块/行内代码/推理与工具卡片背景改为主题感知色，浅色下不再白底白字。
- **同一条回复被拆成多条消息**：会话分组不再因服务端 `turnId` 中途变化而拆散，一条回复合并为单条气泡（一个点赞区）。
- **AI 询问用户（交互）弹窗显示不正确**：按官方 schema 修正权限请求选项、自由输入；新增 `questions` 表单渲染（单选/多选）。

## [0.3.1] - 2026-08-07

### Fixed
- **新建会话首条消息可能发不出去**：普通文本首条消息改为随 `createSession(firstInput)` 一起发送（对齐官方 composer），避免订阅未就绪时命令被丢弃；附件/目标指令路径在发送前等待订阅建立。
- **发送失败不再静默**：`sendText` / `sendGoalCommand` 的 ack 现在会被检查，被拒时提示具体原因。

## [0.3.0] - 2026-08-07

### Added
- **后台任务通知（Android）**：任务运行中时，通知栏静默常驻并实时更新最新进展（前台服务保活）；任务完成静默提醒（低优先级、不弹窗）；点击通知直达对应对话。

## [0.2.1] - 2026-08-06

### Added
- **Skills 支持**：通过 `skills.list` channel 拉取桌面端 Skills，合并进斜杠命令列表（`$` 前缀触发）；新增「选择 Skills」底部弹层，一键填入 `$skillname`。
- **统一 Release 签名**：引入正式 keystore，本地与 CI（GitHub Secrets）使用同一签名，APK 可覆盖安装、支持持续升级。

### Changed
- 输入框：Enter 改为换行，仅通过发送按钮发送；缩短提示文案避免变形。
- 设置页：主题/语言切换按钮改为紧凑小字号，避免变形；新增「检查更新」入口与当前版本展示。
- 状态点颜色改为主题感知，浅色主题下不再不可见。

### Fixed
- 浅色主题下部分文本/图标配色不可读的问题（全面改用主题感知的 `ZInk` 配色）。

## [0.2.0] - 2026-08-05

### Added
- **辅助对话（Side Chat）**：`createSelectionSideSession` 协议支持 + 对话页入口，可开启独立侧对话并行提问。
- **更新检测**：启动自动检查 GitHub 最新发布；Android 端可下载 APK 并调用系统安装器升级（设置页含手动入口）。
- 协议字段扩展：`sendText` / `sendGoalCommand` / `createSession` 新增 `automationId`、`offPeakTaskId`、`botDeliveryTarget`、`runtimeModel`、`mcpServers` 等；sessions-index 新增 `parentSessionId`。
- 对话页展示 `prepareWorkspace` 中的其他配置项（如最大输出长度、搜索增强）。

### Changed
- 设备身份改为真实值（`platform` / `name`），不再伪装为浏览器。
- 版本号升至 `0.2.0+1`。

## [0.1.0] - 2026-08-04

### Added
- 首个发布版本：ZCode 桌面端移动远程控制客户端（协议复刻）。
- 多设备并发连接、扫码/粘贴添加设备。
- 任务列表（任务/置顶/已归档 + 搜索 + 未读标记）。
- Conversation V4 对话：流式回复、推理过程、斜杠命令、模型/模式/思考切换、排队消息、目标指令、附件、diff、回滚、反馈。
- 模型供应商管理、用量/配额/订阅查看。
- 协议调试工具（日志 / RPC / Channel）。
- 浅色/深色主题、字体缩放、中英双语。
